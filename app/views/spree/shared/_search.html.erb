<% @taxons = @taxon && @taxon.parent ? @taxon.parent.children : Spree::Taxon.roots %>
<%= form_tag "/products", :method => :get do %>
    <%= text_field_tag :keywords, params[:keywords], :placeholder => t(:search) %>
    <%= submit_tag t(:search), :name => nil %>
<% end %>
